---
layout: post
title: "进阶 6 —— Docker 容器监控之 CAdvisor + InfluxDB + Granfana"
tags: [Docker]
permalink: /docker_advanced_6.html
---

我们可以通过 docker stats 命令可以很方便的看到当前宿主机上所有容器的 CPU，内存以及网络流量等数据，一般小公司够用了。但是，docker stats 统计结果只能是当前宿主机的全部容器，数据资料是实时的，没有地方存储、没有健康指标过线预警等功能。

![img1](/assets/docker/advanced/Lecture6/img1.png)

## 6.1 CAdvisor InfluxDB Granfana 是什么

CAdvisor 监控收集 + InfluxDB 存储数据 + Granfana 展示图表

![img2](/assets/docker/advanced/Lecture6/img2.png)

**CAdvisor**


CAdvisor 是一个容器资源监控工具，包含了容器的内存，CPU，网络 IO，磁盘 IO 等监控。同时提供了一个 WEB 页面用于查看容器的实时运行状态。CAdvisor 默认存储 2 分钟的数据，而且只是针对单物理机，不过 CAdvisor 提供了很多数据继承接口，支持 InfluxDB，Redis，Kafka，ElasticSearch 等集成，可以加上对应的配置将监控数据发往这些数据库存储起来。

CAdvisor 功能主要有 2 点：

1. 展示 Host 和容器两个层次的监控数据
2. 展示历史变化数据

**InfluxDB**


InfluxDB 是使用 go 语言编写的一个开源分布式时序，事件和指标数据库，无需外部依赖。CAdvisor 默认只在本机保存最近 2 分钟的数据，为了持久化存储数据和统一收集展示监控数据，需要将数据存储到 InfluxDB 中。InfluxDB 是一个时序数据库，专门用于存储时序相关的数据。很适合存储 CAdvisor 数据。而且 CAdvisor 本身已经提供了继承 InfluxDB 的方法。

InfluxDB 的主要功能：

1. 基于时间序列，支持与时间相关的函数
2. 可度量性，可以实时对大量数据进行计算
3. 它支持任意的事件数据

**Granfana**

Granfana 是一个开源的数据监控分析可视化平台，支持多种数据源配置（支持包括 InfluxDB，MySQL，Elasticsearch 等）和丰富的插件及模板功能。支持图标权限控制和报警。

Granfana 的主要特性：

1. 丰富灵活的图形化选项
2. 可以混合多种风格
3. 支持白天和夜间模式
4. 多个数据源

## 6.2 使用 Docker Compose 搭建 CIG 监控平台

第一步：新建目录

```bash
[root@shjava101 mydocker]# mkdir cig
[root@shjava101 mydocker]# pwd
[root@shjava101 mydocker]# cd cig
[root@shjava101 cig]# pwd
/mydocker/cig
```

第二步：新建 `docker-compose.yml`

```yml
version: '3.1'

volumes:
    grafana_data: {}

services:
    influxdb:
        image: tutum/influxdb:0.9
        restart: always
        environment:
            - PRE_CREATE_DB=cadvisor
        ports:
            - "8083:8083"
            - "8086:8086"
        volumes:
            - ./data/influxdb:/data

    cadvisor:
        image: google/cadvisor
        links:
            - influxdb:influxsrv
        command: -storage_driver=influxdb -storage_driver_db=cadvisor -storage_driver_host=influxsrv:8086
        restart: always
        ports:
            - "8080:8080"
        volumes:
            - /:/rootfs:ro
            - /var/run:/var/run:rw
            - /sys:/sys:ro
            - /var/lib/docker/:/var/lib/docker:ro

grafana:
    user: "104"
    image: grafana/grafana
    user: "104"
    restart: always
    links:
        - influxdb:influxsrv
    ports:
        - "3000:3000"
    volumes:
        - grafana_data:/var/lib/grafana
    environment:
        - HTTP_USER=admin
        - HTTP_PASS=admin
        - INFLUXDB_HOST=influxsrv
        - INFLUXDB_PORT=8086
        - INFLUXDB_NAME=cadvisor
        - INFLUXDB_USER=root
        - INFLUXDB_PASS=root
```

第三步：启动 docker-compose 文件

```bash
[root@shjava101 cig]# docker-compose config -q # 检查 docker-compose.yml 文件是否有语法错误
[root@shjava101 cig]# docker-compose up
```

如果启动成功，效果如下：

![img3](/assets/docker/advanced/Lecture6/img3.png)

我们查看三个启动的容器实例：

![img4](/assets/docker/advanced/Lecture6/img4.png)

## 6.3 访问服务

### 6.3.1 访问 CAdvisor 收集服务

使用 ip 地址 + 8080 端口进行访问（一定要注意 tomcat 8080 端口没有占用）。第一次访问比较慢，耐心等待。

![img5](/assets/docker/advanced/Lecture6/img5.png)

### 6.3.2 访问 InfluxDB 存储服务

使用 ip 地址 + 8083 端口进行访问

![img6](/assets/docker/advanced/Lecture6/img6.png)

### 6.3.3 访问 Granfana 展现服务

使用 ip 地址 + 3000 端口进行访问

![img7](/assets/docker/advanced/Lecture6/img7.png)

默认用户名和密码都是 admin。登录以后进入到主页：

![img8](/assets/docker/advanced/Lecture6/img8.png)

我们要通过 Granfana 展现数据，那么数据源在哪里呢？我们需要先配置数据源。

![img9](/assets/docker/advanced/Lecture6/img9.png)

选中数据源 DataSource，然后我们添加数据源：

![img10](/assets/docker/advanced/Lecture6/img10.png)

我们选择 InfluxDB 数据源

![img11](/assets/docker/advanced/Lecture6/img11.png)

点击 select 之后，我们进入具体的配置细节：

![img12](/assets/docker/advanced/lecture6/img12.png)

接下来配置数据库名称以及用户名和密码

![img13](/assets/docker/advanced/Lecture6/img13.png)

点击 Save & test 测试。接下来我们配置面板。

![img14](/assets/docker/advanced/Lecture6/img14.png)

![img15](/assets/docker/advanced/Lecture6/img15.png)

我们选择数据展示格式，是折线图，还是柱状图展示等：

![img16](/assets/docker/advanced/Lecture6/img16.png)

我们选择最经典的展示规则：

![img17](/assets/docker/advanced/Lecture6/img17.png)

最后我们保存设置。

![img18](/assets/docker/advanced/Lecture6/img18.png)

![img19](/assets/docker/advanced/Lecture6/img19.png)

保存之后，效果如下：

![img20](/assets/docker/advanced/Lecture6/img20.png)

现在我们的面板上并没有展示数据，接下来我们对其进行继续配置：

我们对面板进行编辑：

![img21](/assets/docker/advanced/Lecture6/img21.png)

往下拉，继续配置：

![img22](/assets/docker/advanced/Lecture6/img22.png)

容器名称可以在这里看：

```bash
[root@shjava101 cig]# docker stats
```

最后效果如下：

![img23](/assets/docker/advanced/Lecture6/img23.png)

最后保存日志监控信息。

![img24](/assets/docker/advanced/Lecture6/img24.png)

最后监控效果如下：

![img25](/assets/docker/advanced/Lecture6/img25.png)

到这里 CAdvisor + InfluxDB + Granfana 容器监控系统就部署完成了